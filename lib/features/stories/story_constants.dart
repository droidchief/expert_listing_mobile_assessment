/// Single source of truth for how long a story stays on screen. Drives both
/// the progress-bar animation and the auto-advance timer — change it here
/// only.
const Duration kStoryDuration = Duration(seconds: 5);
